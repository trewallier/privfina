{
  "name": "privfina",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-hosted cash-flow modeling engine for personal finance planning.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.7",
    "@types/node": "^25.9.1"
  }
}
